import SwiftUI

struct StartView: View {
    var body: some View {
        Text("Start Screen")
            .font(.largeTitle)
            .foregroundColor(.blue)
    }
}

struct AddView: View {
    var body: some View {
        Text("Add Screen")
            .font(.largeTitle)
            .foregroundColor(.green)
    }
}

struct SearchView: View {
    var body: some View {
        Text("Search Screen")
            .font(.largeTitle)
            .foregroundColor(.red)
    }
}

